import React from 'react';

//this component as the name suggests, represents the heading of the app
const Header = props =>{
    return(
        <header>
            <h1>Roorkee's Famous Food Stops</h1>
        </header>
    );
};

export default Header;
